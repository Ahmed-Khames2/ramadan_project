import 'package:flutter/material.dart';

class KhatamPlannerPage extends StatelessWidget {
  const KhatamPlannerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Khatam Planner')),
      body: const Center(
        child: Text('Khatam Planner - Coming from existing implementation'),
      ),
    );
  }
}
